import { html } from 'lit';
import { Story } from '../../types/storybook';
import { {{sentence (replace name "flp-" "")}} } from './{{name}}.types';
import './{{name}}';
import './../../themes/light.css';
import './../../themes/dark.css';

export default {
  title: 'components/{{sentence (replace name "flp-" "")}}',
  component: '{{name}}',
}

const defaultProps: {{sentence (replace name "flp-" "")}} = {};

const Template: Story<{{sentence (replace name "flp-" "")}}> = (
  props: {{sentence (replace name "flp-" "")}},
  {
    globals: { theme },
  }: {
    globals: { theme: string };
  }
) => html`
  <div class="flp-theme-light">
    <{{name}}>
      {{sentence (replace name "flp-" "")}}
    </{{name}}>
  </div>
`;

export const Default = Template.bind({});
Default.args = defaultProps;
