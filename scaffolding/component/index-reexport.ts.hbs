import { Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}} as Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}}Raw } from './{{name}}.js';
import * as React from 'react';
import { EventName, createComponent } from '@lit/react';

declare global {
  interface HTMLElementTagNameMap {
    '{{name}}': Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}}Raw;
  }
}

const Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}} = createComponent({
  tagName: '{{name}}',
  elementClass: Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}}Raw,
  react: React,
  events: {
  },
  displayName: 'Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}}'
});

export {
  Flp{{pascalcase (replace (replace name "flp-" "") "-" " ")}}
};