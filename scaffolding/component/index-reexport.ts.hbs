import { Flp{{sentence (replace name "flp-" "")}} as Flp{{sentence (replace name "flp-" "")}}Raw } from './{{name}}.js';
import * as React from 'react';
import { EventName, createComponent } from '@lit/react';

declare global {
  interface HTMLElementTagNameMap {
    '{{name}}': Flp{{sentence (replace name "flp-" "")}}Raw;
  }
}

const Flp{{sentence (replace name "flp-" "")}} = createComponent({
  tagName: '{{name}}',
  elementClass: Flp{{sentence (replace name "flp-" "")}}Raw,
  react: React,
  events: {
  },
  displayName: 'Flp{{sentence (replace name "flp-" "")}}'
});

export {
  Flp{{sentence (replace name "flp-" "")}}
};